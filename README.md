# CodeMind-CLI
CodeMind CLI is a distributed, real-time code indexing system designed to replace traditional monolithic indexing pipelines with a continuous synchronization model. Instead of performing expensive full-repository scans, CodeMind incrementally processes only the changes introduced in each commit and updates a centralized knowledge graph.
